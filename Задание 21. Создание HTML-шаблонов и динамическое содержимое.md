# Практическое занятие 21: Создание HTML-шаблонов и динамическое содержимое

**Цель занятия:**
- Создать HTML-шаблоны для различных страниц.
- Реализовать динамическое отображение данных из базы данных.

---

## План занятия

1. **Введение в шаблоны Django (10 минут)**
2. **Создание базового шаблона `base.html` (20 минут)**
3. **Создание наследуемых шаблонов (20 минут)**
4. **Реализация динамического содержимого (20 минут)**
5. **Создание дополнительных динамических элементов (15 минут)**
6. **Запуск и проверка приложения (10 минут)**
7. **Практическое упражнение: Добавление нового шаблона с динамическими данными (5 минут)**
8. **Дополнительное задание**

---

## Задача 1: Введение в шаблоны Django (10 минут)

### 1.1. Важность HTML-шаблонов в Django

- **Шаблоны** позволяют отделить логику приложения от представления данных пользователю.
- Позволяют создавать динамические веб-страницы, используя данные из базы данных.
- Обеспечивают переиспользование кода через механизм наследования шаблонов.

### 1.2. Обзор системы шаблонов Django

- **Django Template Language (DTL)** — встроенный язык шаблонов Django.
- Позволяет использовать теги, фильтры и переменные для отображения динамического контента.
- Поддерживает наследование шаблонов, включение других шаблонов и использование контекстных процессоров.

---

## Задача 2: Создание базового шаблона `base.html` (20 минут)

### 2.1. Создание директории для шаблонов

- Убедитесь, что у вас есть директория `templates/events/` в вашем приложении `events`.
- Если нет, создайте её:
  ```
  your_project/
  ├── events/
  │   ├── templates/
  │   │   └── events/
  │   │       └── (шаблоны будут здесь)
  ```

### 2.2. Создание файла `base.html`

- В директории `events/templates/events/` создайте файл `base.html`.

### 2.3. Наполнение файла `base.html`

```html
<!-- events/templates/events/base.html -->

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Система управления мероприятиями{% endblock %}</title>
    {% load static %}
    <link rel="stylesheet" href="{% static 'events/css/styles.css' %}">
</head>
<body>
    <nav>
        <a href="{% url 'events:home' %}">Главная</a> |
        <a href="{% url 'events:services' %}">Услуги</a> |
        <a href="{% url 'events:team' %}">Команда</a> |
        <a href="{% url 'events:gallery' %}">Галерея</a> |
        <a href="{% url 'events:contact_us' %}">Свяжитесь с нами</a>
    </nav>
    <div class="content">
        {% block content %}
        <!-- Основной контент будет вставлен здесь -->
        {% endblock %}
    </div>
</body>
</html>
```

**Пояснение:**

- **`{% block title %}{% endblock %}`**: Позволяет переопределять заголовок страницы в дочерних шаблонах.
- **`{% block content %}{% endblock %}`**: Место, где будет вставлен основной контент из дочерних шаблонов.
- **`{% load static %}`**: Загружает тег `static` для доступа к статическим файлам.
- **Навигация**: Использует теги `{% url %}` для создания ссылок на маршруты по их именам.

### 2.4. Обновление файла стилей `styles.css`

- Перейдите в директорию для статических файлов: `events/static/events/css/`.
- Обновите файл `styles.css` в этой директории.

**Пример содержимого `styles.css`:**

```css
/* events/static/events/css/styles.css */

body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

nav {
    background-color: #333;
    padding: 10px;
}

nav a {
    color: #ffffff;
    margin-right: 15px;
    text-decoration: none;
}

nav a:hover {
    text-decoration: underline;
}

.content {
    padding: 20px;
}

h1, h2 {
    color: #333;
}

ul {
    list-style-type: none;
    padding: 0;
}

li {
    margin-bottom: 10px;
}
```

### 2.5. Настройка статических файлов в `settings.py`

- Откройте `your_project/settings.py` и убедитесь, что настройки для статических файлов указаны правильно:

```python
# your_project/settings.py

STATIC_URL = '/static/'
STATICFILES_DIRS = [BASE_DIR / 'events/static']
```

---

## Задача 3: Создание наследуемых шаблонов (20 минут)

### 3.1. Обновление шаблона `event_list.html`

**Шаги:**

1. Откройте файл `events/templates/events/event_list.html`.
2. Измените его содержимое следующим образом:

```html
<!-- events/templates/events/event_list.html -->

{% extends 'events/base.html' %}

{% block title %}Список мероприятий{% endblock %}

{% block content %}
    <h1>Мероприятия</h1>
    <ul>
        {% for event in page_obj.object_list %}
            <li>
                <a href="{% url 'events:event_detail' event.id %}">{{ event.title }}</a> -
                {{ event.date|date:"d.m.Y H:i" }}
            </li>
        {% empty %}
            <li>Нет доступных мероприятий.</li>
        {% endfor %}
    </ul>

    <!-- Пагинация -->
    <div class="pagination">
        <span class="step-links">
            {% if page_obj.has_previous %}
                <a href="?page=1">&laquo; Первая</a>
                <a href="?page={{ page_obj.previous_page_number }}">Назад</a>
            {% endif %}

            <span class="current">
                Страница {{ page_obj.number }} из {{ page_obj.paginator.num_pages }}
            </span>

            {% if page_obj.has_next %}
                <a href="?page={{ page_obj.next_page_number }}">Вперед</a>
                <a href="?page={{ page_obj.paginator.num_pages }}">Последняя &raquo;</a>
            {% endif %}
        </span>
    </div>
{% endblock %}
```

**Пояснение:**

- **`{% extends 'events/base.html' %}`**: Указывает, что этот шаблон наследует `base.html`.
- **Использование `page_obj`**: Для отображения списка мероприятий с пагинацией.
- **Пагинация**: Добавлена секция для навигации по страницам.

### 3.2. Обновление других шаблонов

Повторите аналогичные шаги для следующих шаблонов:

#### 3.2.1. `event_detail.html`

```html
<!-- events/templates/events/event_detail.html -->

{% extends 'events/base.html' %}

{% block title %}{{ event.title }}{% endblock %}

{% block content %}
    <h1>{{ event.title }}</h1>
    <p><strong>Дата и время:</strong> {{ event.date|date:"d.m.Y H:i" }}</p>
    <p><strong>Место проведения:</strong> {{ event.location }}</p>
    <p><strong>Описание:</strong> {{ event.description }}</p>
    <p><strong>Категория:</strong> {{ event.category.name }}</p>
    <p><strong>Организатор:</strong> {{ event.organizer.username }}</p>

    <!-- Отображение отзывов -->
    <h2>Отзывы</h2>
    <ul>
        {% for review in event.reviews.all %}
            <li>
                <strong>{{ review.user.username }}:</strong> {{ review.content }}
                (Оценка: {{ review.rating }})
            </li>
        {% empty %}
            <li>Пока нет отзывов.</li>
        {% endfor %}
    </ul>
{% endblock %}
```

#### 3.2.2. `services.html`

```html
<!-- events/templates/events/services.html -->

{% extends 'events/base.html' %}

{% block title %}Услуги{% endblock %}

{% block content %}
    <h1>Наши услуги</h1>
    <ul>
        {% for service in services %}
            <li>{{ service }}</li>
        {% endfor %}
    </ul>
{% endblock %}
```

#### 3.2.3. `team.html`

```html
<!-- events/templates/events/team.html -->

{% extends 'events/base.html' %}

{% block title %}Команда{% endblock %}

{% block content %}
    <h1>Наша команда</h1>
    <ul>
        {% for member in team %}
            <li>{{ member.name }} - {{ member.position }}</li>
        {% endfor %}
    </ul>
{% endblock %}
```

#### 3.2.4. `gallery.html`

```html
<!-- events/templates/events/gallery.html -->

{% extends 'events/base.html' %}

{% block title %}Галерея{% endblock %}

{% block content %}
    <h1>Галерея мероприятий</h1>
    <!-- Контент галереи -->
{% endblock %}
```

#### 3.2.5. `home.html`

```html
<!-- events/templates/events/home.html -->

{% extends 'events/base.html' %}

{% block title %}Домашняя страница{% endblock %}

{% block content %}
    <h1>Добро пожаловать на сайт мероприятий!</h1>
    <!-- Контент домашней страницы -->
{% endblock %}
```

#### 3.2.6. `contact_us.html`

```html
<!-- events/templates/events/contact_us.html -->

{% extends 'events/base.html' %}

{% block title %}Свяжитесь с нами{% endblock %}

{% block content %}
    <h1>Свяжитесь с нами</h1>
    <p>Здесь вы можете разместить форму обратной связи или контактную информацию.</p>
    <!-- Контент страницы обратной связи-->
{% endblock %}
```

### 3.3. Сохраните изменения

- Убедитесь, что вы сохранили все изменения в шаблонах.

---

## Задача 4: Реализация динамического содержимого (20 минут)

### 4.1. Обновление представления `event_list`

**Шаги:**

1. Откройте файл `events/views.py`.
2. Измените функцию `event_list` следующим образом:

```python
# events/views.py

from django.shortcuts import render
from .models import Event, Category
from django.core.paginator import Paginator

def event_list(request):
    events = Event.objects.all().order_by('date')
    paginator = Paginator(events, 5)  # 5 мероприятий на страницу
    page_number = request.GET.get('page')
    page_obj = paginator.get_page(page_number)
    categories = Category.objects.all()
    return render(request, 'events/event_list.html', {
        'page_obj': page_obj,
        'categories': categories
    })
```

**Пояснение:**

- **`Event.objects.all().order_by('date')`**: Получение всех мероприятий, отсортированных по дате.
- **Пагинация**: Используем `Paginator` для разбивки списка на страницы по 5 мероприятий.
- **`page_obj`**: Объект текущей страницы, который передается в шаблон.
- **`categories`**: Передаем категории для отображения в фильтрах (если будут использоваться).

### 4.2. Использование переданных данных в шаблоне `event_list.html`

Убедитесь, что вы используете `page_obj` в цикле `{% for event in page_obj.object_list %}`.

**Пример:**

```html
{% for event in page_obj.object_list %}
    <!-- Ваш код -->
{% endfor %}
```

---

## Задача 5: Создание дополнительных динамических элементов (15 минут)

### 5.1. Добавление отображения категорий в фильтре

**Шаги:**

1. **Добавьте форму фильтрации по категориям в `event_list.html`:**

```html
<!-- events/templates/events/event_list.html -->

{% block content %}
    <h1>Мероприятия</h1>

    <!-- Форма фильтрации -->
    <form method="get">
        <label for="category">Категория:</label>
        <select name="category" id="category">
            <option value="">Все</option>
            {% for category in categories %}
                <option value="{{ category.id }}">{{ category.name }}</option>
            {% endfor %}
        </select>
        <button type="submit">Фильтровать</button>
    </form>

    <!-- Список мероприятий -->
    <ul>
        {% for event in page_obj.object_list %}
            <!-- Ваш код -->
        {% endfor %}
    </ul>

    <!-- Пагинация -->
    <!-- Ваш код -->
{% endblock %}
```

2. **Обновите представление `event_list` для обработки фильтрации:**

```python
# events/views.py

def event_list(request):
    category_id = request.GET.get('category')
    events = Event.objects.all().order_by('date')
    if category_id:
        events = events.filter(category_id=category_id)
    # Остальной код без изменений
```

### 5.2. Отображение отзывов и комментариев на странице мероприятия

**Шаги:**

1. **Откройте шаблон `event_detail.html`.**
2. **Добавьте секцию для отображения отзывов:**

```html
<!-- events/templates/events/event_detail.html -->

<!-- ... Ваш существующий код ... -->

<!-- Отображение отзывов -->
<h2>Отзывы</h2>
<ul>
    {% for review in event.reviews.all %}
        <li>
            <strong>{{ review.user.username }}:</strong> {{ review.content }}
            (Оценка: {{ review.rating }})
        </li>
    {% empty %}
        <li>Пока нет отзывов.</li>
    {% endfor %}
</ul>

<!-- Форма добавления отзыва (если пользователь аутентифицирован) -->
{% if user.is_authenticated %}
    <h3>Оставить отзыв</h3>
    <form method="post">
        {% csrf_token %}
        {{ review_form.as_p }}
        <button type="submit">Отправить</button>
    </form>
{% else %}
    <p><a href="{% url 'events:login' %}">Войдите</a>, чтобы оставить отзыв.</p>
{% endif %}
```

3. **Обновите представление `event_detail` для обработки формы отзыва:**

```python
# events/views.py

from .forms import ReviewForm

def event_detail(request, event_id):
    event = get_object_or_404(Event, id=event_id)
    if request.method == 'POST':
        review_form = ReviewForm(request.POST)
        if review_form.is_valid():
            review = review_form.save(commit=False)
            review.event = event
            review.user = request.user
            review.save()
            return redirect('events:event_detail', event_id=event.id)
    else:
        review_form = ReviewForm()
    return render(request, 'events/event_detail.html', {
        'event': event,
        'review_form': review_form
    })
```

4. **Создайте форму `ReviewForm` в `events/forms.py`:**

```python
# events/forms.py

from django import forms
from .models import Review

class ReviewForm(forms.ModelForm):
    class Meta:
        model = Review
        fields = ['content', 'rating']
```

---

## Задача 6: Запуск и проверка приложения (10 минут)

### 6.1. Применение миграций (если вы добавили новые модели)

- **Создайте миграции:**

```bash
python manage.py makemigrations
```

- **Примените миграции:**

```bash
python manage.py migrate
```

### 6.2. Запуск сервера разработки

```bash
python manage.py runserver
```

### 6.3. Проверка работы приложения

- **Перейдите на страницу списка мероприятий:**

  - [http://127.0.0.1:8000/events/](http://127.0.0.1:8000/events/)

- **Убедитесь, что мероприятия отображаются корректно.**

- **Проверьте фильтрацию по категориям, выбрав категорию из выпадающего списка и нажав "Фильтровать".**

- **Перейдите на страницу конкретного мероприятия и проверьте отображение отзывов и возможность оставить новый отзыв.**

---

## Задача 7: Практическое упражнение (5 минут)

### **Задание для студентов:**

- Создайте новый шаблон `profile.html`, наследующийся от `base.html`.
- Отобразите в нём информацию о текущем пользователе: имя пользователя, email и список мероприятий, на которые он зарегистрирован.

**Шаги:**

1. **Создайте шаблон `profile.html` в `events/templates/events/`:**

```html
<!-- events/templates/events/profile.html -->

{% extends 'events/base.html' %}

{% block title %}Профиль{% endblock %}

{% block content %}
    <h1>Профиль пользователя</h1>
    <p><strong>Имя пользователя:</strong> {{ user.username }}</p>
    <p><strong>Email:</strong> {{ user.email }}</p>

    <h2>Мои мероприятия</h2>
    <ul>
        {% for registration in registrations %}
            <li>
                <a href="{% url 'events:event_detail' registration.event.id %}">{{ registration.event.title }}</a>
                - {{ registration.event.date|date:"d.m.Y H:i" }}
            </li>
        {% empty %}
            <li>Вы не зарегистрированы ни на одно мероприятие.</li>
        {% endfor %}
    </ul>
{% endblock %}
```

2. **Добавьте представление `profile` в `events/views.py`:**

```python
# events/views.py

from django.contrib.auth.decorators import login_required
from .models import Registration

@login_required
def profile(request):
    registrations = Registration.objects.filter(user=request.user)
    return render(request, 'events/profile.html', {
        'registrations': registrations
    })
```

3. **Добавьте маршрут в `events/urls.py`:**

```python
# events/urls.py

urlpatterns = [
    # ... другие маршруты ...
    path('profile/', views.profile, name='profile'),
]
```

4. **Проверьте работу, перейдя по адресу:**

- [http://127.0.0.1:8000/profile/](http://127.0.0.1:8000/profile/)

---

## Дополнительное задание

### **1. Реализация механизма поиска мероприятий**

**Цель:** Добавить возможность поиска мероприятий по названию.

**Шаги:**

1. **Добавьте форму поиска в `event_list.html`:**

```html
<!-- events/templates/events/event_list.html -->

{% block content %}
    <h1>Мероприятия</h1>

    <!-- Форма поиска -->
    <form method="get">
        <input type="text" name="q" placeholder="Поиск по названию" value="{{ request.GET.q }}">
        <button type="submit">Поиск</button>
    </form>

    <!-- Остальная часть шаблона -->
    <!-- ... -->
```

2. **Обновите представление `event_list` для обработки поисковых запросов:**

```python
# events/views.py

from django.db.models import Q

def event_list(request):
    query = request.GET.get('q')
    category_id = request.GET.get('category')
    events = Event.objects.all().order_by('date')
    if query:
        events = events.filter(Q(title__icontains=query) | Q(description__icontains=query))
    if category_id:
        events = events.filter(category_id=category_id)
    # Остальной код без изменений
```

3. **Проверьте работу поиска:**

- Введите текст в поле поиска и нажмите "Поиск".
- Убедитесь, что отображаются мероприятия, соответствующие запросу.

### **2. Добавление блоков в шаблоны**

**Цель:** Создать дополнительные блоки в `base.html`, такие как `sidebar` и `footer`, и использовать их в наследуемых шаблонах.

**Шаги:**

1. **Обновите `base.html`, добавив новые блоки:**

```html
<!-- events/templates/events/base.html -->

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Система управления мероприятиями{% endblock %}</title>
    {% load static %}
    <link rel="stylesheet" href="{% static 'events/css/styles.css' %}">
</head>
<body>
    <nav>
        <!-- Навигация -->
    </nav>
    <div class="content">
        {% block content %}
        <!-- Основной контент -->
        {% endblock %}
    </div>
    <div class="sidebar">
        {% block sidebar %}
        <!-- Боковая панель (по умолчанию пустая) -->
        {% endblock %}
    </div>
    <footer>
        {% block footer %}
        <p>&copy; 2023 Система управления мероприятиями</p>
        {% endblock %}
    </footer>
</body>
</html>
```

2. **Используйте новые блоки в дочерних шаблонах (например, `event_list.html`):**

```html
<!-- events/templates/events/event_list.html -->

{% extends 'events/base.html' %}

{% block title %}Список мероприятий{% endblock %}

{% block content %}
    <!-- Ваш основной контент -->
{% endblock %}

{% block sidebar %}
    <h2>Категории</h2>
    <ul>
        {% for category in categories %}
            <li><a href="?category={{ category.id }}">{{ category.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}

{% block footer %}
    <p>&copy; 2023 Система управления мероприятиями. Все права защищены.</p>
{% endblock %}
```

3. **Обновите CSS для оформления новых блоков (если необходимо).**

---

## Заключение и вопросы

**Поздравляем! Вы успешно:**

- Создали базовый шаблон `base.html` и настроили наследование шаблонов.
- Обновили существующие шаблоны для использования `base.html`.
- Реализовали динамическое отображение данных из базы данных.
- Добавили функциональность фильтрации и поиска мероприятий.
- Использовали дополнительные блоки в шаблонах для расширения функциональности.

**Вопросы для обсуждения:**

- **Как механизм наследования шаблонов помогает в разработке?**
  - Позволяет переиспользовать общие элементы интерфейса, сокращает дублирование кода, облегчает внесение изменений.

- **Какие преимущества дает использование пагинации и фильтрации?**
  - Улучшает пользовательский опыт, позволяет удобно просматривать большое количество данных, снижает нагрузку на сервер.

- **Как можно дальше улучшить функциональность сайта?**
  - Добавить аутентификацию и авторизацию, возможность регистрации на мероприятия, систему уведомлений, интеграцию с платежными системами и т.д.

---

## Полезные ресурсы

- [Документация Django: Шаблоны](https://docs.djangoproject.com/en/4.2/topics/templates/)
- [Django Template Language (DTL) Cheatsheet](https://docs.djangoproject.com/en/4.2/ref/templates/language/)
- [Django Пагинация](https://docs.djangoproject.com/en/4.2/topics/pagination/)
- [Документация по классам представлений](https://docs.djangoproject.com/en/4.2/topics/class-based-views/)

---

Если у вас возникнут дополнительные вопросы или потребуется помощь, не стесняйтесь обращаться к преподавателю или обсуждать с одногруппниками.

**Успехов в разработке!**